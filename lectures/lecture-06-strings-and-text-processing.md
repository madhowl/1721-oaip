# Лекция 6: Строки и работа с текстом

## Введение

Строки - это одна из самых важных структур данных в программировании, особенно в языке Python. Они используются для представления текстовой информации имеют множество встроенных методов для манипуляции с текстом. В этой лекции мы подробно рассмотрим, как работать со строками в Python, какие операции можно выполнять и как использовать различные методы для обработки текста.

## Основное содержание

### Что такое строки в Python

Строка (string) в Python - это неизменяемая последовательность символов, заключенная в кавычки (одинарные, двойные или тройные). Строки в Python относятся к категории последовательностей, что означает, что к ним можно обращаться по индексу, использовать срезы и применять к ним итерации.

```python
# Создание строк различными способами
single_quotes = 'Привет'
double_quotes = "Мир"
triple_quotes = """Это многострочная
строка"""
raw_string = r"Специальные символы \n не интерпретируются"
```

### Индексация строк

Каждый символ в строке имеет свой индекс - позицию в строке. В Python индексация начинается с 0 для первого символа, а отрицательные индексы используются для доступа к символам с конца строки.

```python
text = "Python"
print(text[0])    # 'P' - первый символ
print(text[5])    # 'n' - шестой символ
print(text[-1])   # 'n' - последний символ
print(text[-2])   # 'o' - предпоследний символ
```

### Срезы строк (slicing)

Срезы позволяют извлекать подстроки из строки, используя синтаксис `[start:end:step]`, где:
- `start` - начальный индекс (включительно)
- `end` - конечный индекс (не включительно)
- `step` - шаг (по умолчанию 1)

```python
text = "Программирование"
print(text[0:7])     # "Програм" - с 0 по 7 символы
print(text[7:])      # "мирование" - с 7 символа до конца
print(text[:7])      # "Програм" - с начала до 7 символа
print(text[:])       # "Программирование" - вся строка
print(text[::2])     # "Прармрвне" - каждый второй символ
print(text[::-1])    # "еенимворгарП" - строка в обратном порядке
```

### Основные операции со строками

#### Конкатенация строк

Конкатенация - это операция объединения строк с помощью оператора `+`.

```python
first_name = "Иван"
last_name = "Петров"
full_name = first_name + " " + last_name
print(full_name)  # "Иван Петров"
```

#### Повторение строк

Строку можно повторить несколько раз с помощью оператора `*`.

```python
greeting = "Привет! "
print(greeting * 3)  # "Привет! Привет! Привет! "
```

#### Проверка вхождения подстроки

Оператор `in` позволяет проверить, содержится ли одна строка в другой.

```python
text = "Программирование на Python"
print("Python" in text)     # True
print("Java" in text)       # False
print("грам" in text)       # True
```

### Методы строк

Python предоставляет множество методов для работы со строками. Вот некоторые из наиболее часто используемых:

#### Методы для изменения регистра

```python
text = "Привет, Мир!"

print(text.upper())         # "ПРИВЕТ, МИР!"
print(text.lower())         # "привет, мир!"
print(text.capitalize())    # "Привет, мир!" (первая буква заглавная)
print(text.title())         # "Привет, Мир!" (каждое слово с заглавной)
print(text.swapcase())      # "пРИВЕТ, мИР!" (изменяет регистр всех букв)
```

#### Методы для удаления пробелов

```python
text = "   Привет, мир!   "
print(text.strip())         # "Привет, мир!" (удаляет пробелы с обоих концов)
print(text.lstrip())        # "Привет, мир!   " (удаляет пробелы слева)
print(text.rstrip())        # "   Привет, мир!" (удаляет пробелы справа)
```

#### Методы для поиска и замены

```python
text = "Программирование интересно и полезно"

# Поиск подстроки
print(text.find("интересно"))    # 16 (индекс начала подстроки)
print(text.find("Java"))         # -1 (подстрока не найдена)
print(text.index("полезно"))     # 27 (индекс начала подстроки, ошибка если не найдено)

# Замена подстроки
print(text.replace("интересно", "увлекательно"))  # "Программирование увлекательно и полезно"

# Подсчет вхождений
print(text.count("о"))           # 3 (количество букв "о")
```

#### Методы для разделения и объединения строк

```python
# Разделение строки
text = "яблоко, груша, апельсин"
fruits = text.split(", ")        # ['яблоко', 'груша', 'апельсин']
print(fruits)

# Объединение строк
words = ['Python', 'легкий', 'язык']
sentence = " ".join(words)       # "Python легкий язык"
print(sentence)

# Разделение на строки
multiline = "Первая строка\nВторая строка\nТретья строка"
lines = multiline.splitlines()   # ['Первая строка', 'Вторая строка', 'Третья строка']
print(lines)
```

#### Методы для проверки содержимого строки

```python
text1 = "12345"
text2 = "abcde"
text3 = "Abc123"
text4 = "   "
text5 = "Привет Мир"

print(text1.isdigit())      # True (только цифры)
print(text2.isalpha())      # True (только буквы)
print(text3.isalnum())      # True (буквы и цифры)
print(text4.isspace())      # True (только пробельные символы)
print(text5.istitle())      # True (каждое слово с заглавной буквы)
```

### Форматирование строк

В Python есть несколько способов форматирования строк:

#### Метод format()

```python
name = "Алексей"
age = 25
city = "Москва"

# Позиционные аргументы
template = "Меня зовут {}, мне {} лет, я из {}".format(name, age, city)
print(template)  # "Меня зовут Алексей, мне 25 лет, я из Москва"

# Именованные аргументы
template = "Меня зовут {name}, мне {age} лет, я из {city}".format(name=name, age=age, city=city)
print(template)

# Числовое индексирование
template = "Меня зовут {0}, мне {1} лет, я из {2}. Повтор: меня зовут {0}".format(name, age, city)
print(template)
```

#### f-строки (Formatted String Literals)

f-строки были добавлены в Python 3.6 и являются наиболее современным и удобным способом форматирования строк:

```python
name = "Алексей"
age = 25
city = "Москва"

# f-строки
f_string = f"Меня зовут {name}, мне {age} лет, я из {city}"
print(f_string)  # "Меня зовут Алексей, мне 25 лет, я из Москва"

# Можно использовать выражения внутри f-строк
print(f"Через 5 лет мне будет {age + 5} лет")

# Форматирование чисел
price = 1234.5678
print(f"Цена: {price:.2f}")           # "Цена: 1234.57" (2 знака после запятой)
print(f"Цена: {price:>10.2f}")       # "Цена:    1234.57" (выравнивание по правому краю)
print(f"Цена: {price:<10.2f}")       # "Цена: 1234.57   " (выравнивание по левому краю)
```

#### Метод % (старый стиль)

```python
name = "Алексей"
age = 25

old_style = "Меня зовут %s, мне %d лет" % (name, age)
print(old_style)  # "Меня зовут Алексей, мне 25 лет"
```

### Работа с символами Unicode

Python полностью поддерживает Unicode, что позволяет работать с символами из разных языков:

```python
# Русские буквы
russian = "Привет, мир!"
print(russian)

# Смайлики и другие символы
emoji = "Привет! 😊🌍"
print(emoji)

# Получение кода символа и символа по коду
print(ord('A'))     # 65
print(chr(65))      # 'A'
print(ord('а'))     # 1072
print(chr(1072))    # 'а'
```

## Практические примеры

### Пример 1: Валидация email

```python
def validate_email(email):
    # Простая проверка наличия @ и точки
    if "@" in email and "." in email:
        at_index = email.find("@")
        dot_index = email.rfind(".")  # последняя точка
        
        # Проверяем, что @ не в начале и не в конце, и точка после @
        if at_index > 0 and dot_index > at_index and dot_index < len(email) - 1:
            return True
    return False

email = "example@domain.com"
print(f"Email {email} валиден: {validate_email(email)}")
```

### Пример 2: Подсчет слов в тексте

```python
def count_words(text):
    # Удаляем лишние пробелы и разделяем по пробелам
    words = text.strip().split()
    return len(words)

def get_word_frequency(text):
    # Приводим к нижнему регистру и разбиваем на слова
    words = text.lower().split()
    frequency = {}
    
    for word in words:
        # Убираем пунктуацию
        clean_word = word.strip('.,!?";:()[]{}')
        if clean_word:  # если слово не пустое
            frequency[clean_word] = frequency.get(clean_word, 0) + 1
    
    return frequency

text = "Python - это мощный язык программирования. Python легко изучать."
print(f"Количество слов: {count_words(text)}")
print(f"Частота слов: {get_word_frequency(text)}")
```

### Пример 3: Работа с текстовыми шаблонами

```python
def fill_template(template, **kwargs):
    """Заполняет шаблон значениями из словаря"""
    for key, value in kwargs.items():
        template = template.replace(f"{{{key}}}", str(value))
    return template

template = "Уважаемый {name}, ваш заказ #{order_id} будет доставлен {delivery_date}."
result = fill_template(template, name="Иван", order_id=12345, delivery_date="завтра")
print(result)
```

## Заключение

В этой лекции мы подробно рассмотрели работу со строками в Python. Мы узнали о:

1. **Создании строк** различными способами
2. **Индексации и срезах**, которые позволяют обращаться к отдельным символам и подстрокам
3. **Основных операциях** со строками: конкатенации, повторении, проверке вхождения
4. **Методах строк**, которые предоставляют широкие возможности для манипуляции текстом
5. **Форматировании строк** с использованием различных подходов
6. **Работе с Unicode**, что особенно важно для многоязычных приложений

Строки являются фундаментальной частью программирования, и умение эффективно работать с текстом необходимо для большинства приложений. В следующей лекции мы рассмотрим списки и кортежи - важные структуры данных для хранения коллекций элементов.
